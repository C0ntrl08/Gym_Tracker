<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Gym_Tracker.ViewModels"
             x:Class="Gym_Tracker.MainPage">

    <!-- Set the BindingContext to your ViewModel -->
    <ContentPage.BindingContext>
        <vm:ExerciseViewModel />
    </ContentPage.BindingContext>

    <CollectionView ItemsSource="{Binding GroupedExercises}" SelectionMode="Single" IsGrouped="True">
        <CollectionView.GroupHeaderTemplate>
            <DataTemplate>
                <StackLayout BackgroundColor="LightGray" Padding="10">
                    <Label Text="{Binding GroupName}" FontSize="16" FontAttributes="Bold" />
                </StackLayout>
            </DataTemplate>
        </CollectionView.GroupHeaderTemplate>
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="15" />
        </CollectionView.ItemsLayout>
        <CollectionView.HeaderTemplate>
            <DataTemplate>
                <StackLayout BackgroundColor="LightGray">
                    <Label Margin="0,15,0,15"
                       Text="Get in shape with these exercises 🔒"
                       FontSize="12"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center"/>
                </StackLayout>
            </DataTemplate>
        </CollectionView.HeaderTemplate>
        <CollectionView.FooterTemplate>
            <DataTemplate>
                <StackLayout BackgroundColor="LightGray">
                    <Label Margin="0,0,0,0"
                       Text="© 2025 Doriano Benyocki"
                       FontSize="12"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center"/>
                </StackLayout>
            </DataTemplate>
        </CollectionView.FooterTemplate>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid Padding="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.RowSpan="2"
                       Source="{Binding BaseImage}"
                       Aspect="AspectFill"
                       HeightRequest="60"
                       WidthRequest="60" />
                    <Label Grid.Column="1"
                       Text="{Binding Name}"
                       FontAttributes="Bold" />
                    <Label Grid.Row="1"
                       Grid.Column="1"
                       Text="{Binding Description}"
                       FontAttributes="Italic"
                       VerticalOptions="End" />
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
